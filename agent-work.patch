From ca6536bab3d474fb06b98cf7f51971edb50dcb5a Mon Sep 17 00:00:00 2001
From: pi-colab <pi-colab@gmail.com>
Date: Mon Jun 29 2026 04:37:10 +0000
Subject: [PATCH 2/3] feat(core): implement filesystem scaffold for scaffold
 command and expanded scaffold output

---
 src/commands/scaffold.ts | 11 ++++++++++-
 1 file changed, 10 insertions(+), 1 deletion(-)

diff --git a/src/commands/scaffold.ts b/src/commands/scaffold.ts
index 80fd78d..5ca50bf 100644
--- a/src/commands/scaffold.ts
+++ b/src/commands/scaffold.ts
@@ -1,4 +1,13 @@
 export async function scaffoldCmd(name:string):Promise<string>{
   const nm = name || 'project';
-  return `Scaffolded starter for ${nm}`;
+  // Expanded scaffold: write actual starter files to disk
+  const fs = await import('fs');
+  const path = await import('path');
+  const root = path.resolve(process.cwd(), nm);
+  if(!fs.existsSync(root)){
+    fs.mkdirSync(root, {recursive:true});
+    fs.writeFileSync(path.join(root,'README.md'),`# ${nm}\n\nGenerated by locall_llm_unhinged MVP scaffold.`, 'utf8');
+    fs.writeFileSync(path.join(root,'package.json'), JSON.stringify({name:nm, version:'0.1.0', private:true}, null, 2));
+  }
+  return `Scaffolded starter for ${nm} at ${root}`;
 }
--- 
2.34.1


From 2f539e5c0605ae63b5d45596258ff6e6fb0bf3b7 Mon Sep 17 00:00:00 2001
From: pi-colab <pi-colab@gmail.com>
Date: Mon 29 Jun 2026 04:38:05 +0000
Subject: [PATCH 3/3] feat(plugins): add lightweight plugin loader and runner
 stub

---
 .github/workflows/ci.yml |  0
 DEVELOPING.md            |  7 +++++++
 plugins/index.ts         |  2 ++
 src/plugins/runner.ts    | 24 ++++++++++++++++++++++++
 4 files changed, 33 insertions(+)
 create mode 100644 .github/workflows/ci.yml
 create mode 100644 DEVELOPING.md
 create mode 100644 plugins/index.ts
 create mode 100644 src/plugins/runner.ts

diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
new file mode 100644
index 0000000..e69de29
diff --git a/DEVELOPING.md b/DEVELOPING.md
new file mode 100644
index 0000000..7bf1d2f
--- /dev/null
+++ b/DEVELOPING.md
@@ -0,0 +1,7 @@
+# DEVELOPING
+
+Planned enhancements for MVP:
+- Plugin system loader
+- Deterministic offline explain
+- Tests for plugins and scaffold
+- Documentation and CI
diff --git a/plugins/index.ts b/plugins/index.ts
new file mode 100644
index 0000000..dc99ff0
--- /dev/null
+++ b/plugins/index.ts
@@ -0,0 +1,2 @@
+export type Plugin = { name:string; init: ()=>void; };
+export { loadPlugins } from '../src/plugins/runner';
diff --git a/src/plugins/runner.ts b/src/plugins/runner.ts
new file mode 100644
index 0000000..24aed32
--- /dev/null
+++ b/src/plugins/runner.ts
@@ -0,0 +1,24 @@
+export type Plugin = { name: string; init: ()=>void };
+
+export async function loadPlugins(){
+  // Discover JS/TS modules in plugins/ and require/import them dynamically.
+  // For MVP, return a static placeholder if no plugins yet.
+  const path = await import('path');
+  const fs = await import('fs');
+  const pluginsDir = path.resolve(process.cwd(), 'plugins');
+  let plugins: Plugin[] = [];
+  if(fs.existsSync(pluginsDir)){
+    for(const f of fs.readdirSync(pluginsDir)){
+      if(f.endsWith('.js') || f.endsWith('.ts')){
+        try{
+          const mod = await import(path.resolve(pluginsDir, f));
+          if(mod?.default?.name && typeof mod.default?.init === 'function'){
+            plugins.push({ name: mod.default.name, init: mod.default.init });
+            mod.default.init();
+          }
+        }catch(e){ /* ignore bad plugin */ }
+      }
+    }
+  }
+  return plugins;
+}
+
