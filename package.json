{
  "name": "locall_llm_unhinged",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/cli.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^0.34.0",
    "ts-node": "^10.9.1"
  }
}
